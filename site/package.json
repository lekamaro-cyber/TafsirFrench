{
  "name": "tafsir-french",
  "type": "module",
  "version": "1.0.0",
  "description": "Plateforme de publication du Tafsir en francais",
  "scripts": {
    "dev": "node scripts/copy-pdfs.js && astro dev",
    "build": "node scripts/copy-pdfs.js && astro build && node scripts/bundle-worker.js",
    "preview": "astro preview",
    "sync": "node scripts/sync-content.js",
    "publish-scheduled": "node scripts/publish-scheduled.js",
    "copy-pdfs": "node scripts/copy-pdfs.js"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
